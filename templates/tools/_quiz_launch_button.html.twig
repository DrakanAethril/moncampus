{# « Lancer un quiz » at the right of the tab row. The class the screen is filtered on travels with
   it, so a teacher who was already looking at one class finds it picked on the launch form; with
   « Toutes » selected nothing is preselected, which is the same answer as before. #}
<a href="{{ path('app_quiz_launch', programFilter ? {program: programFilter.id} : {}) }}" class="btn btn-primary">
    <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M7 4v16l13 -8z" /></svg>
    {{ 'quizLaunchPageHeading'|trans }}
</a>
