{% extends 'layout/app.html.twig' %}

{% block title %}{{ 'newUserPageHeading'|trans }}{% endblock %}
{% block page_title %}{{ 'newUserPageHeading'|trans }}{% endblock %}

{% block main %}
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card">
                <div class="card-body">
                    {{ form_start(form) }}
                        {{ form_errors(form) }}
                        {{ form_row(form.firstname) }}
                        {{ form_row(form.lastname) }}
                        {{ form_row(form.userType) }}
                        {{ form_row(form.userGroups) }}
                        <div class="d-flex justify-content-between">
                            <a href="{{ path('app_annuaire_utilisateurs') }}" class="btn btn-link">{{ 'cancelAction'|trans }}</a>
                            {{ form_widget(form.submit) }}
                        </div>
                        {{ form_rest(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
