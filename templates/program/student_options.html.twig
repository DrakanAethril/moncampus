{% extends 'layout/app.html.twig' %}

{% block title %}{{ 'studentOptionsPageHeading'|trans }} - {{ student.displayName ?? student.username }}{% endblock %}
{% block page_title %}{{ 'studentOptionsPageHeading'|trans }} - {{ student.displayName ?? student.username }}{% endblock %}

{% block main %}
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card">
                <div class="card-body">
                    {{ form_start(form) }}
                        {{ form_errors(form) }}
                        {{ form_row(form.options) }}
                        <div class="d-flex justify-content-between">
                            <a href="{{ path('app_program_settings_students', {id: program.id}) }}" class="btn btn-link">{{ 'cancelAction'|trans }}</a>
                            {{ form_widget(form.submit) }}
                        </div>
                        {{ form_rest(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
