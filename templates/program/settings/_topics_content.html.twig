{# Same hour-total cards as program/syllabus.html.twig (App\Service\TopicHourStatsCalculator) -
   this tab and the read-only Syllabus page list the same active Topics, just via a settings
   table instead of a plain DataTable view. #}
<div class="row row-cards mb-3">
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <div class="subheader">{{ 'syllabusTotalHoursLabel'|trans }}</div>
                <div class="h3 m-0">{{ totalHours }} H</div>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <div class="subheader">{{ 'syllabusCmHoursLabel'|trans }}</div>
                <div class="h3 m-0">{{ totalCmHours }} H</div>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <div class="subheader">{{ 'syllabusTdHoursLabel'|trans }}</div>
                <div class="h3 m-0">{{ totalTdHours }} H</div>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <div class="subheader">{{ 'syllabusTpHoursLabel'|trans }}</div>
                <div class="h3 m-0">{{ totalTpHours }} H</div>
            </div>
        </div>
    </div>
</div>

{% for stat in optionStats %}
    <div class="d-flex align-items-center gap-2 mb-2">
        <span class="badge" style="background-color: {{ stat.option.color }}; color: #fff;">{{ stat.option.shortName }}</span>
        <h3 class="m-0">{{ stat.option.name }}</h3>
    </div>
    <div class="row row-cards mb-3">
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <div class="subheader">{{ 'syllabusTotalHoursLabel'|trans }}</div>
                    <div class="h3 m-0">{{ stat.totalHours }} H</div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <div class="subheader">{{ 'syllabusCmHoursLabel'|trans }}</div>
                    <div class="h3 m-0">{{ stat.cmHours }} H</div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <div class="subheader">{{ 'syllabusTdHoursLabel'|trans }}</div>
                    <div class="h3 m-0">{{ stat.tdHours }} H</div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <div class="subheader">{{ 'syllabusTpHoursLabel'|trans }}</div>
                    <div class="h3 m-0">{{ stat.tpHours }} H</div>
                </div>
            </div>
        </div>
    </div>
{% endfor %}

<div class="card"
     data-controller="topics-table"
     data-topics-table-option-column-value="6"
     data-topics-table-group-column-value="7"
     data-topics-table-topic-count-label-value="{{ 'syllabusTopicCountLabel'|trans }}"
     data-topics-table-sum-columns-value="[1, 2, 3, 4, 5]"
     data-topics-table-empty-table-label-value="{{ 'topicsEmptyMessage'|trans }}">
    <div class="table-responsive">
        <table class="table card-table table-vcenter text-nowrap" data-topics-table-target="table">
            <thead>
                <tr>
                    <th>{{ 'topicNameColumnLabel'|trans }}</th>
                    <th>{{ 'topicTargetCmHoursColumnLabel'|trans }}</th>
                    <th>{{ 'topicTargetTdHoursColumnLabel'|trans }}</th>
                    <th>{{ 'topicTargetTpHoursColumnLabel'|trans }}</th>
                    <th>{{ 'topicTotalTargetHoursColumnLabel'|trans }}</th>
                    <th>{{ 'topicPlannedHoursColumnLabel'|trans }}</th>
                    {# Two hidden columns follow (sort keys, not shown to the user - the group's own
                       badges/name are only ever shown once, on RowGroup's own header row, not per
                       topic): the group's own Option short name(s) - primary sort - and the group's
                       raw name - RowGroup's grouping key and secondary sort. #}
                    <th></th>
                    <th></th>
                    <th class="cm-actions"></th>
                </tr>
            </thead>
            <tbody>
                {% for topic in topics %}
                    {% set plannedHours = plannedHoursByTopicId[topic.id] ?? 0 %}
                    {% set groupOptions = topic.topicGroup.options|sort((a, b) => a.shortName <=> b.shortName) %}
                    <tr>
                        <td>{{ topic.name }}</td>
                        <td>{{ topic.targetCmHours }}</td>
                        <td>{{ topic.targetTdHours }}</td>
                        <td>{{ topic.targetTpHours }}</td>
                        <td>{{ topic.totalTargetHours }}</td>
                        <td class="fw-bold {{ plannedHours == topic.totalTargetHours ? 'text-success' : 'text-danger' }}">{{ plannedHours }}</td>
                        <td>{{ groupOptions|map(o => o.shortName)|join(', ') }}</td>
                        <td>{{ topic.topicGroup.name }}</td>
                        <td class="cm-actions">
                            <a href="{{ path('app_program_timetable_settings_topics_edit', {id: program.id, topicId: topic.id}) }}" class="cm-action--warning">{{ 'editActionLabel'|trans }}</a>
                            <form method="post" action="{{ path('app_program_timetable_settings_topics_deactivate', {id: program.id, topicId: topic.id}) }}" onsubmit="return confirm('{{ 'deactivateConfirmMessage'|trans|e('js') }}')">
                                <input type="hidden" name="_token" value="{{ csrf_token('program_settings_topics_deactivate') }}">
                                <button type="submit" class="cm-action--danger">{{ 'deactivateActionLabel'|trans }}</button>
                            </form>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
            <tfoot>
                <tr>
                    <th>{{ 'topicGrandTotalLabel'|trans }}</th>
                    <th></th>
                    <th></th>
                    <th></th>
                    <th></th>
                    <th></th>
                    <th></th>
                    <th></th>
                    <th></th>
                </tr>
            </tfoot>
        </table>
    </div>
</div>
