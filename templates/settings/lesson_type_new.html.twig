{% extends 'layout/app.html.twig' %}

{% block title %}{{ (isEdit ? 'editLessonTypePageHeading' : 'newLessonTypePageHeading')|trans }}{% endblock %}
{% block page_title %}{{ (isEdit ? 'editLessonTypePageHeading' : 'newLessonTypePageHeading')|trans }}{% endblock %}

{% block main %}
    <div class="card">
        <div class="card-body">
            {{ form_start(form) }}
                {{ form_errors(form) }}
                <div class="row">
                    <div class="col-md-6">{{ form_row(form.name) }}</div>
                    <div class="col-md-6">{{ form_row(form.agendaColor) }}</div>
                    <div class="col-md-6">{{ form_row(form.defaultCost) }}</div>
                </div>
                <div class="d-flex justify-content-between">
                    <a href="{{ path('app_settings_structure_lesson_types') }}" class="btn btn-link">{{ 'cancelAction'|trans }}</a>
                    {{ form_widget(form.submit, {'label': isEdit ? 'submitEditAction' : 'submitCreateAction'}) }}
                </div>
                {{ form_rest(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
