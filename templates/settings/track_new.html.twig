{% extends 'layout/app.html.twig' %}

{% block title %}{{ 'newTrackPageHeading'|trans }}{% endblock %}
{% block page_title %}{{ 'newTrackPageHeading'|trans }}{% endblock %}

{% block main %}
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card">
                <div class="card-body">
                    {{ form_start(form) }}
                        {{ form_errors(form) }}
                        {{ form_row(form.name) }}
                        {{ form_row(form.section) }}
                        {{ form_row(form.ldapGroup) }}
                        <div class="d-flex justify-content-between">
                            <a href="{{ path('app_settings_structure') }}" class="btn btn-link">{{ 'cancelAction'|trans }}</a>
                            {{ form_widget(form.submit) }}
                        </div>
                        {{ form_rest(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
