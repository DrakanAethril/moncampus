{# 4a's primary action, rendered inside the filter bar - a plain link, deliberately not a submit,
   so clicking it never carries the filter form's query string over to the management table. #}
<a href="{{ path('app_progression_manage') }}" class="cm-btn cm-btn--primary">{{ 'progressionManageAction'|trans }}</a>
