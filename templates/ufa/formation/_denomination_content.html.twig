{# 24b matches the old path's 8a layout, so the shared partial is reused as-is; only the
   "Annuler" destination differs (stay on this UFA tab). #}
{% include 'program/internship/_denomination_content.html.twig' with {backRoute: 'app_ufa_formation_denomination'} %}
