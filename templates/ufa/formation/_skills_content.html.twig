{# « Compétences » - the very partial Formation > Paramétrage renders under the name « Groupes de
   compétences ». It is included rather than copied: the two tabs are one screen behind two menus,
   and the route names it draws come from the controller (skillRoutes). #}
{% include 'program/settings/_skill_groups_content.html.twig' %}
