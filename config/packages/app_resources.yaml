# Deployment metadata for the "Ressources" mobile-app download pages (App\Controller\ResourcesController).
# Version/date/size are declarative here rather than computed from the actual APK/IPA files, since
# those files are dropped onto the server independently of a deploy (see public/downloads/README.md) -
# bump these values by hand (or wire them to CI later) whenever a new build is published.
parameters:
    app.resources:
        moncampus:
            name: 'MonCampus'
            version: '1.0.0'
            updated_at: '2026-07-17'
            android_size: '47 Mo'
            ios_size: '44 Mo'
            ios_bundle_id: 'fr.beaupeyrat.moncampus'
        eco:
            name: 'e-CO'
            version: '1.0.0'
            updated_at: '2026-07-17'
            android_size: '71 Mo'
            ios_size: '36 Mo'
            ios_bundle_id: 'fr.beaupeyrat.eco'
