# Overrides/extends the "security" domain shipped by symfony/security-core
# (vendor/symfony/security-core/Resources/translations/security.en.xlf, which only covers its own
# built-in exception classes) - login.html.twig renders any AuthenticationException via
# error.messageKey|trans(error.messageData, 'security'), and App\Security\MagicLinkAuthenticator's
# CustomUserMessageAuthenticationException needs its own key resolved here.
magicLoginConsumeInvalidMessage: 'This login link is invalid, expired, or has already been used.'
