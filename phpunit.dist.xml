<?xml version="1.0" encoding="UTF-8"?>

<!-- https://phpunit.readthedocs.io/en/latest/configuration.html -->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true"
         bootstrap="tests/bootstrap.php"
         cacheDirectory=".phpunit.cache"
>
    <php>
        <ini name="display_errors" value="1" />
        <ini name="error_reporting" value="-1" />
        <!-- The dev container's CLI runs at php.ini-development's default 128M (only
             frankenphp/conf.d/20-app.prod.ini raises it, and only for prod), which the functional
             tests outgrew: building the test kernel's container costs ~190M on its own, so
             RoleAccessSmokeTest died mid-boot on "Premature end of PHP process" - an out-of-memory
             fatal with no usable message, since it happens inside the container builder's deep
             clone. A ceiling rather than -1, so a genuine runaway in a test still fails instead of
             taking the machine down with it.

             Raised from 512M on 2026-09-16, when the suite passed 2 900 tests: PHPUnit keeps a
             result object per test for the whole run, so the process now sits near 500M by the last
             third of the suite - and the two tests that build a deliberately oversized upload
             (PdfUploadValidatorTest, QuizImportImageValidatorTest) then died of "Allowed memory size
             exhausted" on a five-megabyte str_repeat. Nothing leaked; the headroom had simply run
             out, and the failure landed on whichever test happened to allocate next. -->
        <ini name="memory_limit" value="1G" />
        <server name="APP_ENV" value="test" force="true" />
        <server name="SHELL_VERBOSITY" value="-1" />
    </php>

    <testsuites>
        <testsuite name="Project Test Suite">
            <directory>tests</directory>
        </testsuite>
    </testsuites>

    <source ignoreSuppressionOfDeprecations="true"
            ignoreIndirectDeprecations="true"
            restrictNotices="true"
            restrictWarnings="true"
    >
        <include>
            <directory>src</directory>
        </include>

        <deprecationTrigger>
            <method>Doctrine\Deprecations\Deprecation::trigger</method>
            <method>Doctrine\Deprecations\Deprecation::delegateTriggerToBackend</method>
            <function>trigger_deprecation</function>
        </deprecationTrigger>
    </source>

    <extensions>
    </extensions>
</phpunit>
